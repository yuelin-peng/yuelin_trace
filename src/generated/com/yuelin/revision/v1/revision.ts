// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v4.25.9
// source: com/yuelin/revision/v1/revision.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../../google/protobuf/timestamp";
import { PageRequest, PageResponse } from "../../common/v1/page";

export const protobufPackage = "com.yuelin.revision.v1";

/** / Revision represents a version snapshot of an article for history tracking. */
export interface Revision {
  /** / Unique identifier (UUID) */
  id: string;
  /** / Associated article ID */
  articleId: string;
  /** / Article content at this revision */
  content: string;
  /** / Author user ID who created this revision */
  authorId: string;
  /** / Creation timestamp */
  createdAt: Date | undefined;
}

/** / ListRevisionsRequest for listing article revisions with pagination. */
export interface ListRevisionsRequest {
  /** / Article ID to list revisions for */
  articleId: string;
  /** / Pagination request */
  pageRequest:
    | PageRequest
    | undefined;
  /** / Sort order (asc for oldest first, desc for newest first) */
  sortOrder: string;
}

/** / ListRevisionsResponse returns list of revisions with pagination. */
export interface ListRevisionsResponse {
  /** / List of revisions */
  revisions: Revision[];
  /** / Pagination response */
  pageResponse: PageResponse | undefined;
}

/** / GetRevisionRequest for fetching a single revision. */
export interface GetRevisionRequest {
  /** / Revision ID */
  id: string;
}

/** / GetRevisionResponse returns the requested revision. */
export interface GetRevisionResponse {
  revision: Revision | undefined;
}

/** / RestoreRevisionRequest for restoring an article from a revision. */
export interface RestoreRevisionRequest {
  /** / Revision ID to restore from */
  revisionId: string;
  /** / New title for the restored article (optional) */
  title: string;
}

/** / RestoreRevisionResponse returns restoration metadata. */
export interface RestoreRevisionResponse {
  /** / Restored article ID */
  articleId: string;
  /** / Source revision ID */
  revisionId: string;
  /** / Restoration timestamp */
  restoredAt: Date | undefined;
}

function createBaseRevision(): Revision {
  return { id: "", articleId: "", content: "", authorId: "", createdAt: undefined };
}

export const Revision: MessageFns<Revision> = {
  encode(message: Revision, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.articleId !== "") {
      writer.uint32(18).string(message.articleId);
    }
    if (message.content !== "") {
      writer.uint32(26).string(message.content);
    }
    if (message.authorId !== "") {
      writer.uint32(34).string(message.authorId);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Revision {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevision();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.articleId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.authorId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Revision {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      articleId: isSet(object.articleId)
        ? globalThis.String(object.articleId)
        : isSet(object.article_id)
        ? globalThis.String(object.article_id)
        : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      authorId: isSet(object.authorId)
        ? globalThis.String(object.authorId)
        : isSet(object.author_id)
        ? globalThis.String(object.author_id)
        : "",
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
    };
  },

  toJSON(message: Revision): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.articleId !== "") {
      obj.articleId = message.articleId;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.authorId !== "") {
      obj.authorId = message.authorId;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Revision>, I>>(base?: I): Revision {
    return Revision.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Revision>, I>>(object: I): Revision {
    const message = createBaseRevision();
    message.id = object.id ?? "";
    message.articleId = object.articleId ?? "";
    message.content = object.content ?? "";
    message.authorId = object.authorId ?? "";
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function createBaseListRevisionsRequest(): ListRevisionsRequest {
  return { articleId: "", pageRequest: undefined, sortOrder: "" };
}

export const ListRevisionsRequest: MessageFns<ListRevisionsRequest> = {
  encode(message: ListRevisionsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.articleId !== "") {
      writer.uint32(10).string(message.articleId);
    }
    if (message.pageRequest !== undefined) {
      PageRequest.encode(message.pageRequest, writer.uint32(18).fork()).join();
    }
    if (message.sortOrder !== "") {
      writer.uint32(26).string(message.sortOrder);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRevisionsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRevisionsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.articleId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pageRequest = PageRequest.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sortOrder = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListRevisionsRequest {
    return {
      articleId: isSet(object.articleId)
        ? globalThis.String(object.articleId)
        : isSet(object.article_id)
        ? globalThis.String(object.article_id)
        : "",
      pageRequest: isSet(object.pageRequest)
        ? PageRequest.fromJSON(object.pageRequest)
        : isSet(object.page_request)
        ? PageRequest.fromJSON(object.page_request)
        : undefined,
      sortOrder: isSet(object.sortOrder)
        ? globalThis.String(object.sortOrder)
        : isSet(object.sort_order)
        ? globalThis.String(object.sort_order)
        : "",
    };
  },

  toJSON(message: ListRevisionsRequest): unknown {
    const obj: any = {};
    if (message.articleId !== "") {
      obj.articleId = message.articleId;
    }
    if (message.pageRequest !== undefined) {
      obj.pageRequest = PageRequest.toJSON(message.pageRequest);
    }
    if (message.sortOrder !== "") {
      obj.sortOrder = message.sortOrder;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListRevisionsRequest>, I>>(base?: I): ListRevisionsRequest {
    return ListRevisionsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListRevisionsRequest>, I>>(object: I): ListRevisionsRequest {
    const message = createBaseListRevisionsRequest();
    message.articleId = object.articleId ?? "";
    message.pageRequest = (object.pageRequest !== undefined && object.pageRequest !== null)
      ? PageRequest.fromPartial(object.pageRequest)
      : undefined;
    message.sortOrder = object.sortOrder ?? "";
    return message;
  },
};

function createBaseListRevisionsResponse(): ListRevisionsResponse {
  return { revisions: [], pageResponse: undefined };
}

export const ListRevisionsResponse: MessageFns<ListRevisionsResponse> = {
  encode(message: ListRevisionsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.revisions) {
      Revision.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pageResponse !== undefined) {
      PageResponse.encode(message.pageResponse, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRevisionsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRevisionsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.revisions.push(Revision.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pageResponse = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListRevisionsResponse {
    return {
      revisions: globalThis.Array.isArray(object?.revisions)
        ? object.revisions.map((e: any) => Revision.fromJSON(e))
        : [],
      pageResponse: isSet(object.pageResponse)
        ? PageResponse.fromJSON(object.pageResponse)
        : isSet(object.page_response)
        ? PageResponse.fromJSON(object.page_response)
        : undefined,
    };
  },

  toJSON(message: ListRevisionsResponse): unknown {
    const obj: any = {};
    if (message.revisions?.length) {
      obj.revisions = message.revisions.map((e) => Revision.toJSON(e));
    }
    if (message.pageResponse !== undefined) {
      obj.pageResponse = PageResponse.toJSON(message.pageResponse);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListRevisionsResponse>, I>>(base?: I): ListRevisionsResponse {
    return ListRevisionsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListRevisionsResponse>, I>>(object: I): ListRevisionsResponse {
    const message = createBaseListRevisionsResponse();
    message.revisions = object.revisions?.map((e) => Revision.fromPartial(e)) || [];
    message.pageResponse = (object.pageResponse !== undefined && object.pageResponse !== null)
      ? PageResponse.fromPartial(object.pageResponse)
      : undefined;
    return message;
  },
};

function createBaseGetRevisionRequest(): GetRevisionRequest {
  return { id: "" };
}

export const GetRevisionRequest: MessageFns<GetRevisionRequest> = {
  encode(message: GetRevisionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetRevisionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRevisionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetRevisionRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: GetRevisionRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetRevisionRequest>, I>>(base?: I): GetRevisionRequest {
    return GetRevisionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetRevisionRequest>, I>>(object: I): GetRevisionRequest {
    const message = createBaseGetRevisionRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseGetRevisionResponse(): GetRevisionResponse {
  return { revision: undefined };
}

export const GetRevisionResponse: MessageFns<GetRevisionResponse> = {
  encode(message: GetRevisionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.revision !== undefined) {
      Revision.encode(message.revision, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetRevisionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRevisionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.revision = Revision.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetRevisionResponse {
    return { revision: isSet(object.revision) ? Revision.fromJSON(object.revision) : undefined };
  },

  toJSON(message: GetRevisionResponse): unknown {
    const obj: any = {};
    if (message.revision !== undefined) {
      obj.revision = Revision.toJSON(message.revision);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetRevisionResponse>, I>>(base?: I): GetRevisionResponse {
    return GetRevisionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetRevisionResponse>, I>>(object: I): GetRevisionResponse {
    const message = createBaseGetRevisionResponse();
    message.revision = (object.revision !== undefined && object.revision !== null)
      ? Revision.fromPartial(object.revision)
      : undefined;
    return message;
  },
};

function createBaseRestoreRevisionRequest(): RestoreRevisionRequest {
  return { revisionId: "", title: "" };
}

export const RestoreRevisionRequest: MessageFns<RestoreRevisionRequest> = {
  encode(message: RestoreRevisionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.revisionId !== "") {
      writer.uint32(10).string(message.revisionId);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RestoreRevisionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRestoreRevisionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.revisionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RestoreRevisionRequest {
    return {
      revisionId: isSet(object.revisionId)
        ? globalThis.String(object.revisionId)
        : isSet(object.revision_id)
        ? globalThis.String(object.revision_id)
        : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
    };
  },

  toJSON(message: RestoreRevisionRequest): unknown {
    const obj: any = {};
    if (message.revisionId !== "") {
      obj.revisionId = message.revisionId;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RestoreRevisionRequest>, I>>(base?: I): RestoreRevisionRequest {
    return RestoreRevisionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RestoreRevisionRequest>, I>>(object: I): RestoreRevisionRequest {
    const message = createBaseRestoreRevisionRequest();
    message.revisionId = object.revisionId ?? "";
    message.title = object.title ?? "";
    return message;
  },
};

function createBaseRestoreRevisionResponse(): RestoreRevisionResponse {
  return { articleId: "", revisionId: "", restoredAt: undefined };
}

export const RestoreRevisionResponse: MessageFns<RestoreRevisionResponse> = {
  encode(message: RestoreRevisionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.articleId !== "") {
      writer.uint32(10).string(message.articleId);
    }
    if (message.revisionId !== "") {
      writer.uint32(18).string(message.revisionId);
    }
    if (message.restoredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.restoredAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RestoreRevisionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRestoreRevisionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.articleId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.revisionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.restoredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RestoreRevisionResponse {
    return {
      articleId: isSet(object.articleId)
        ? globalThis.String(object.articleId)
        : isSet(object.article_id)
        ? globalThis.String(object.article_id)
        : "",
      revisionId: isSet(object.revisionId)
        ? globalThis.String(object.revisionId)
        : isSet(object.revision_id)
        ? globalThis.String(object.revision_id)
        : "",
      restoredAt: isSet(object.restoredAt)
        ? fromJsonTimestamp(object.restoredAt)
        : isSet(object.restored_at)
        ? fromJsonTimestamp(object.restored_at)
        : undefined,
    };
  },

  toJSON(message: RestoreRevisionResponse): unknown {
    const obj: any = {};
    if (message.articleId !== "") {
      obj.articleId = message.articleId;
    }
    if (message.revisionId !== "") {
      obj.revisionId = message.revisionId;
    }
    if (message.restoredAt !== undefined) {
      obj.restoredAt = message.restoredAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RestoreRevisionResponse>, I>>(base?: I): RestoreRevisionResponse {
    return RestoreRevisionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RestoreRevisionResponse>, I>>(object: I): RestoreRevisionResponse {
    const message = createBaseRestoreRevisionResponse();
    message.articleId = object.articleId ?? "";
    message.revisionId = object.revisionId ?? "";
    message.restoredAt = object.restoredAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
